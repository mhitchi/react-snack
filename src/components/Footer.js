import React from "react";

class Footer extends React.Component {
    render() {
        return (
            //shorthand for React.Fragment, which allows multiple children without wrapping in parent div
            <>
            Footer
            </>
        )
    }
};

export default Footer;