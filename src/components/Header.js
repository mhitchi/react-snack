import React from "react";
import "../css/Style.scss";

class Header extends React.Component {
    render() {
        return (
            //shorthand for React.Fracture, which allows multiple children without wrapping in parent div
            <>
            Header
            </>
        )
    }
};

export default Header;
